# Демо‑сайт «WebStudio» — как запустить

## Структура
- `index.html` — главная страница (тарифы, примеры, заявка)
- `styles.css` — стили
- `script.js` — поведение (скролл, выбор тарифа)
- `contact.php` — обработчик формы (отправка на email)
- `favicon.svg` — иконка

## Что изменить под себя (минимум)
1) В файле `contact.php` замените строку:
```
$to = "you@example.com";
```
на вашу почту.

2) В `index.html` замените тексты, название студии и цены при необходимости.

## Загрузка на хостинг (ISPmanager + FileZilla)
1. В ISPmanager зайдите в файловый менеджер и откройте папку вашего домена:
   `/var/www/www-root/data/www/ВАШ-ДОМЕН` (или путь из панели).
2. Загрузите все файлы из архива в корень сайта.
3. Убедитесь, что главная страница называется `index.html`.
4. Если форма не отправляет письма, а хостинг не поддерживает `mail()`:
   - подключите SMTP в панели (часто в разделе «Почта»/«PHP»), либо
   - временно замените `action="contact.php"` на:
     `action="mailto:you@example.com"` (менее надёжно), либо
   - напишите мне — добавлю отправку через SMTP (PHPMailer).

## SSL (Let's Encrypt)
- В ISPmanager откройте «WWW‑домены» → выберите домен → вкладка «SSL» → включите «Let's Encrypt» и согласитесь на выпуск.
- Если система просит TXT‑запись `_acme-challenge`, добавьте её у регистратора домена и подождите валидацию.

## Частые вопросы
- **Где менять цены/тарифы?** — Блок «Тарифы» в `index.html`.
- **Как добавить демо‑сайты в портфолио?** — В секции `#portfolio` можно дублировать карточки `.shot`.
- **Нужна CMS (WordPress)?** — Я могу подготовить версию на WP с админкой по запросу.

Удачных продаж! 🚀
